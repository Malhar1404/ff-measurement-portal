import { Box } from "@react-three/drei"
import { Canvas } from "@react-three/fiber"
export const Canvas3d = ()=>{
    return (
        <Canvas>
            <Box/>
            
        </Canvas>
    )
}